// 1.3 Añade un evento 'input' que ejecute un console.log con el valor del input.

const myInput = document.querySelector('input');

myInput.addEventListener('input', fuction(e)){
    console.log(myInput.value);
}